# 实验1：RISC-V引导和裸机启动

## 任务1：理解xv6启动流程

### kernel/entry.S - 启动汇编代码

- 重点理解：栈设置、BSS清零、跳转到C代码 
- 思考：为什么需要设置栈？栈应该多大？ 

为什么需要设置栈？

- 因为整个系统代码执行过程中需要使用栈空间（一般的，程序使用栈空间存放函数的额外参数，保存变量的值（溢出到栈上的寄存器变量，函数调用时调用者/被调用者的保存）等）

为什么第一条指令是设置栈指针？（栈指针设计分析）

- 栈指针和栈空间的设置过程如下：
  - 将栈指针sp设为stack0：`la sp, stack0 `
    - la 为加载地址的伪指令，这里将stack0的地址递给sp
    - stack0：在start.c中定义：`__attribute__ ((aligned (16))) char stack0[4096 * NCPU]; `
      - 其定义了一个16字节对齐，4096*NCPU（8）字节的内存空间，start0是这个数组的头指针
  - 将a0设为1024*4：`li a0, 1024*4`，将a1设为当前CPU的线程号：`csrr a1, mhartid`
    - 每个CPU分配4096字节的栈空间
    - csrr指令用于读取状态寄存器的值输入到寄存器中
    - mhartid表示当前CPU线程的编号，标识这个CPU的核编号（0，1……）
  - 计算当前CPU栈底地址，并将其输入到
    - 这个每个CPU线程的栈空间并排放在一起
    - 由于栈空间的延伸方向为高地址到低地址，所以其底
    - sp = stack0 + (hartid * 4096)
    - 通过给SP进行add操作定位到当前栈底（表示栈空）

为什么要清零BSS段？

- BSS段：未初始化或初始化为0的全局变量和静态变量，没必要放在ROM上，只需记录起始地址和大小，再使用前清零即可
- 为了使得那些初始化为0的静态变量能够为0，需要清零（考虑到内存内可能有污染，部分值初始不为0，如果直接使用，可能使用到了非0数据，导致出错）

- 问题：在entry.S段中没有任何BSS清零的迹象，在kernel.ld里声明了.bss，其没有清零的作用
  - 清零的时机：启动代码时（通常在start.S内，在本例中没找到），程序主逻辑开始之前
  - 还是没有找到清零的代在项目代码中的什么位置……

如何从汇编跳转到C函数？
- `call start`调用在start.C中的start函数
- 问题：有关于RISC-V如何实现跨文件调用函数的

### kernel/kernel.ld - 链接脚本

- 重点理解：入口点设置、段的组织、符号定义 
- 思考：各个段的作用和排列顺序

ENTRY(_entry) 的作用是什么？ 

- OUTPUT_ARCH("riscv")：指定输出内核镜像的目标架构为 ​RISC-V。这确保了链接器会生成符合 RISC-V 指令集规范的二进制代码
。
- ENTRY(_entry)：指定程序的入口点​（Entry Point）为符号 _entry。这意味着当模拟器（如 QEMU）或引导加载程序加载内核后，执行的第一条指令就是 _entry所在的地址。这个符号通常定义在汇编文件（如 entry.S）中，是内核启动代码的起点

为什么代码段要放在0x80000000？ 

- 在其中`. = 0x80000000;`这个部分设置了定位计数器​（Location Counter） .的值为 0x80000000。定位计数器可以理解为链接器的“当前指针”，后续段的地址会以此为基础进行分配
- 在许多 RISC-V 系统中（特别是在 QEMU 模拟环境下），0x80000000是内核加载的起始物理地址。将代码段的起始地址设置于此，确保了内核能被正确加载和执行

etext、edata、end 符号有什么用途？

etext: 设置为当前的定位计数器值,用于计算代码段的大小或确定其他段的起始位置(.text代码段 和 .rodata 只读数据段的结束，.data数据段的开始)
edata: 类似于etext。其标记了data数据段的结束地址，可以在内核中计算数据段的大小，起始位置
end: 这标记了整个内核镜像（包括所有程序段）的结束地址()


- etext：PROVIDE(etext = .);：定义了一个符号 etext，并将其值设置为当前的定位计数器值。这标记了代码段（包括蹦床代码）的结束地址，可以在内核中用于计算代码段的大小或确定其他段的起始位置
- edata：类似于etext。其标记了data段的结束地址，可以在内核中计算数据段的大小，起始位置
  - 问题：edata在kernel.ld中没有出现；取而代之的，无论是data还是rodata，都知识定义了对应段并一股脑的把所有文件扔进去（16字节对齐），仅此而已
- end：PROVIDE(end = .);：定义了一个符号 end，其值为当前定位计数器的值。这标记了整个内核镜像（包括所有段）的结束地址

### 任务1深入思考： 
xv6支持多核，你的单核系统可以如何简化？ 

如果单核可以考虑如下：

方法： 因为实验是单核，所以只需要设计了一个4096的栈空间固定（在Entry.s）（但是可以进一步优化）
       如果实验是多核，sp = stack0 + stack_size * hartid(线程数，第几个核) (通常为4096)

- entry.S无需获取当前CPU线程号，仅需要得到大小即可。sp = stack0 + stack_size * hartid(线程数，第几个核) (通常为4096)

xv6的内存管理很复杂，最小系统需要哪些部分？

- 代码段，数据段，堆栈段（放在bss里）等

## 任务2：设计最小启动流程

启动流程方案：
1. entry_S确定栈指针地址，对每一个线程开对应的栈空间
2. start.c设置页表项为0即禁止分类，将mepc设为main函数的地址，为管理操作系统的物理空间等。最后使用mret功能跳转到main函数
3. main函数对于众多函数内容做初始化处理，并且根据当前线程不同进行处理，并通过串口输出对应信息

关键问题： 

- 栈应该放在内存的哪个位置？需要多大？
  - 放在stack0中，每个线程分配4096字节
- 是否需要清零BSS段？为什么？ 
  - 无需，因为代码内已配置，BSS段内一般为全0
- 最简单串口输出需要配置哪些寄存器？ 
  - pmpaddr0，pmpcfg0（作为最基本的寄存器，作为其分配）
    - 问题：这里没有那么清楚就是了
  - 串口相关寄存器：

## 任务3：实现启动汇编代码

本部分如代码所示

## 任务4：编写链接脚本

本部分如代码所示

## 任务5：实现串口驱动



### kernel/uart.c - 串口驱动

- 重点理解：UART寄存器操作、字符输出实现 
- 思考：如何简化为最小实现？

## 任务6：完成C主函数

## 额外任务：锁的处理

S模式，M模式，F模式

对内核构建页表，启用页表后，只要程序执行不混乱，仍然把
需要用测试代码测试内存分配的正确性